# Build output. The binary is anchored to the repository root so that the
# cmd/godrop package is not swept up with it.
/bin/
/dist/
/godrop

# Local state
/data/
.env
*.local

# Copied into the site at build time by the Cloudflare deploy
/site/install.sh
node_modules/

# Coverage
coverage.out
coverage.html

# Editors and platforms
.DS_Store
.idea/
.vscode/
